- binary_sensor:
    - name: "Workshop dør låst"
      unique_id: doorlock_workshop_closed
      device_class: lock
      state: >-
        {{ not is_state('binary_sensor.doorlock_workshop', 'locked') }}
